apply plugin: 'org.gint.atlassian'
apply from: 'common/utils.gradle'

def info = gint.getJiraHelper().getServerInfoWithVerify() // Verify access to Jira instance otherwise end test if server not available
def project = gint.getJiraHelper().getProjectKey()	//get project key using jira helper functions
def workflow = project
def startStep = info ?.isCloud ?  '1'  : '11' // depends on workflow. '1' for cloud and '11' (To Do)for server works for custom jira workflow
def transition = 'relatedIssuesStatusCondition'

def type = "condition"
def functionKey = 'com.innovalog.jmwe.jira-misc-workflow-extensions:linkedissuesstatus-condition'

(1..13).each { number ->
    gint.taskHelper.addSetUp(
        action: 'addTransition', ext: number,
        end: true,
        parameters: [
            workflow: workflow,
            name: transition + number,
            description: 'Transition for relatedIssuesStatusCondition' + number,
            step: startStep,
        ],
        data: [
            "Transition '${transition + number}' added to workflow '${workflow}' with id ",
        ],
    )
}
(1..13).each { number ->
def relatedIssuesStatusCondition
    if (number == 1)
    	relatedIssuesStatusCondition =  "{\"conditionMode\":\"all-in-status\",\"selectedLinkType\":\"outward:10100\",\"issue_statuses\":\"10000\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do
     else if (number == 2)
        relatedIssuesStatusCondition =  "{\"conditionMode\":\"all-in-status\",\"selectedLinkType\":\"inward:10100\",\"issue_statuses\":\"10000\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do
     else if (number == 3)
        relatedIssuesStatusCondition = "{\"conditionMode\":\"all-in-status\",\"selectedLinkType\":\"outward:10200\",\"issue_statuses\":\"10000\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do
     else if (number == 4)
        relatedIssuesStatusCondition = "{\"conditionMode\":\"all-in-status\",\"selectedLinkType\":\"inward:10200\",\"issue_statuses\":\"10000\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do
     else if (number == 5)
        relatedIssuesStatusCondition = "{\"conditionMode\":\"all-in-status\",\"selectedLinkType\":\"*:*\",\"issue_statuses\":\"10000\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do
     else if (number == 6)
        relatedIssuesStatusCondition = "{\"conditionMode\":\"all-in-status\",\"selectedLinkType\":\"outward:10000\",\"issue_statuses\":\"10000\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do
     else if (number == 7)
        relatedIssuesStatusCondition = "{\"conditionMode\":\"all-in-status\",\"selectedLinkType\":\"groovy:issue.getLinkedIssues()\",\"issue_statuses\":\"10000\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do
     else if (number == 8)
        relatedIssuesStatusCondition = "{\"conditionMode\":\"all-in-status\",\"selectedLinkType\":\"jql:project='${project}'\",\"issue_statuses\":\"10000\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do
//--------------------------------------------------//-----------------------------------------------//--------------------------------------//-------------------------------//-------------------------------------------//------------------------------//
     else if (number == 9)
        relatedIssuesStatusCondition = "{\"conditionMode\":\"all-in-status\",\"selectedLinkType\":\"outward:10100\",\"issue_statuses\":\"\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do
     else if (number == 10)
        relatedIssuesStatusCondition = "{\"conditionMode\":\"\",\"selectedLinkType\":\"outward:10100\",\"issue_statuses\":\"10000\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do
     else if (number == 11)
        relatedIssuesStatusCondition = "{\"conditionMode\":\"\",\"selectedLinkType\":\"outward:10100\",\"issue_statuses\":\"\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do
     else if (number == 12)
        relatedIssuesStatusCondition = "{\"conditionMode\":\"all-in-status\",\"selectedLinkType\":\"\",\"issue_statuses\":\"\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do
     else if (number == 13)
        relatedIssuesStatusCondition = "{\"conditionMode\":\"\",\"selectedLinkType\":\"\",\"issue_statuses\":\"\",\"correlationId\":\"\"}"	//The issue must be in one of the following statuses: To Do

gint.taskHelper.addSetUp(
	action: 'addTransitionFunction', ext: 'RelatedIssuesStatusCondition'+number,
        dependsOn: true,
        end: true, // one at a time
        parameters: [
            workflow: workflow,
            transition: transition + number,
            functionKey: functionKey,
            type: type,
            step: startStep,
            data: relatedIssuesStatusCondition,
        ],
        data: [
            "Transition function of type ${type} added to workflow '${workflow}'.",
        ],
    )
}